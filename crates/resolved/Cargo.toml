[package]
name = "systemd-resolved"
version = "0.1.0"
authors = ["Niclas Overby <niclas@overby.me>"]
edition = "2024"

[[bin]]
name = "systemd-resolved"
path = "src/main.rs"

[dependencies]
chrono = "0.4"
fern = "0.7"
libc = "0.2"
log = "0.4"
nix = { version = "0.31", features = [
  "net",
  "poll",
  "process",
  "signal",
  "socket",
] }
signal-hook = "0.4"
zbus = "5"

[dev-dependencies]
tempfile = "3"
