[package]
name = "systemd-homed"
version = "0.1.0"
authors = ["Niclas Overby <niclas@overby.me>"]
edition = "2024"

[[bin]]
name = "systemd-homed"
path = "src/main.rs"

[dependencies]
zbus = "5"
libc = "0.2"
log = "0.4"
nix = { version = "0.31", features = ["signal", "process", "user", "fs"] }

[dev-dependencies]
tempfile = "3"
