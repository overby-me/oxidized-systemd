# Phase 5 — Utilities, Boot & Polish

Remaining components and production readiness:

- ✅ **`analyze`** — boot performance analysis with `blame`, `time`, `critical-chain`, `dot`, `calendar`, `timespan`, `timestamp`, `verify`, `condition`, `unit-paths`, `security`, `log-level`, `log-target`, `service-watchdogs`, `plot` (SVG boot timeline with color-coded unit bars, phase summary, time axis, and legend), `inspect-elf` (ELF binary metadata extraction — class, endianness, machine type, OS/ABI, build-id from `.note.gnu.build-id`, package metadata from `.note.package` FDO notes, interpreter from `.interp`), `fdstore` (file descriptor store inspection for services with `FileDescriptorStoreMax=`), `image-policy` (image dissection policy parsing and normalization with 13 partition designations and 7 use policies, plus `allow`/`deny`/`ignore` shortcuts), `pcrs` (TPM2 Platform Configuration Register values from sysfs with SHA-256/SHA-1/SHA-384/SHA-512 bank support and per-register descriptions), `srk` (TPM2 Storage Root Key information with device detection, supported hash algorithms, and optional `tpm2-tools` integration) subcommands
- ✅ **`cgls`** / ✅ **`cgtop`** — cgroup tree listing with process display; real-time cgroup resource monitor with CPU/memory/I/O tracking, sorting, batch mode
- ✅ **`mount`** / **`umount`** — transient mount/automount unit creation, mount table listing, filesystem mount/unmount with force/lazy options
- ✅ **`socket-activate`** — socket activation testing tool; creates TCP/UDP/Unix listening sockets, passes FDs via sd_listen_fds(3) protocol (`LISTEN_FDS`/`LISTEN_PID`/`LISTEN_FDNAMES`), `--accept` mode for per-connection spawning, `--datagram` for UDP, `--recv-buffer`/`--backlog`/`--foreground` options
- ✅ **`ac-power`** — AC power state detection
- ✅ **`detect-virt`** — virtualization/container detection
- ✅ **`sd-boot`** / **`bootctl`** — UEFI boot manager and control tool; `bootctl` userspace tool with `status`, `list`, `install`, `update`, `remove`, `is-installed`, `set-default`, `set-oneshot`, `set-timeout`, `set-timeout-oneshot`, `random-seed`, `reboot-to-firmware`, `systemd-efi-options`, `kernel-identify`, `kernel-inspect`, `cleanup`, `entry-directory`, `unlink` subcommands; EFI variable read/write via efivarfs with UTF-16LE encoding/decoding and immutable flag handling; ESP/XBOOTLDR auto-discovery from `/efi`, `/boot`, `/boot/efi` with VFAT and loader directory detection; Boot Loader Specification Type #1 entry parsing (`.conf` drop-in files with title/version/linux/initrd/options/devicetree/sort-key) and Type #2 entry discovery (Unified Kernel Images from `EFI/Linux/*.efi`); boot counting support (`+tries_left-tries_done` filename parsing); PE/COFF section parser for UKI metadata extraction (`.osrel`, `.uname`, `.cmdline`); kernel image identification (UKI, vmlinuz/bzImage/ARM64 Image/ARM zImage/RISC-V/gzip); `loader.conf` parsing (default, timeout, console-mode, editor, auto-entries, auto-firmware, beep, secure-boot-enroll, random-seed-mode); version-aware entry sorting with numeric segment comparison; random seed management with entropy accumulation and system token; auto-detection of Windows Boot Manager, EFI Shell, rEFInd; firmware info reporting (Secure Boot, Setup Mode, TPM2, OsIndications); loader feature flag decoding (14 feature bits); `sd-boot` UEFI boot manager (`no_std`/`no_main` PE application targeting `x86_64-unknown-uefi`/`aarch64-unknown-uefi`) with BLS Type #1/Type #2 entry discovery, `loader.conf` parsing, graphical boot menu with keyboard navigation (Up/Down/Home/End/Enter/Escape), auto-boot with configurable timeout, boot counting with automatic fallback, random seed processing with system token XOR, EFI variable publication (LoaderInfo/LoaderFeatures/LoaderEntrySelected/LoaderFirmwareType), oneshot entry consumption, firmware setup reboot via OsIndications, auto-detection of Windows/EFI Shell entries; 68 unit tests
- ✅ **`sd-stub`** — UEFI stub for unified kernel images (`no_std`/`no_main` PE application); extracts `.linux`, `.initrd`, `.cmdline`, `.osrel`, `.uname`, `.splash`, `.dtb`, `.sbat`, `.pcrpkey`, `.pcrsig` sections from own PE image; passes initrd to kernel via `EFI_LOAD_FILE2_PROTOCOL` on Linux initrd vendor media device path; combines embedded `.cmdline` with boot loader load options; TPM2 PCR measurement of all UKI sections (PCR 8 kernel cmdline, PCR 9 kernel image, PCR 11 unified image, PCR 13 sysexts) via TCG2 protocol; publishes `StubInfo`/`StubFeatures`/`LoaderOsRelease`/`LoaderKernelVersion` EFI variables; BMP splash screen display via Graphics Output Protocol; credential discovery from `\loader\credentials\` with CPIO archive packaging; system extension discovery from `\loader\sysext\*.raw` with PCR 13 measurement; CPIO newc archive builder for appending credentials/sysexts to initrd; device path parsing for own image file location; `os-release` PRETTY_NAME extraction
- ✅ **Generator framework** — fstab and getty generators built natively into `libsystemd`; external generator execution framework discovers and runs all standard generators (`systemd-gpt-auto-generator`, `systemd-cryptsetup-generator`, `systemd-debug-generator`, `systemd-run-generator`, etc.) from well-known directories plus package-relative paths; output directories inserted at correct unit search path priorities; built-in generators automatically skipped; per-generator timeout with graceful failure handling
- 🔶 **Comprehensive test suite** — 7,475 unit tests passing; integration tests via nixos-rs boot test; differential testing against real systemd tracked in [Phase 6](phase-6.md)
- ❌ **Documentation** — man-page-compatible documentation for all binaries and configuration formats
- 🔶 **NixOS / distro integration** — packaging via `default.nix`, boot testing via `test-boot.sh`, NixOS module via `systemd.nix`; working end-to-end; udev rules override ensures correct `systemctl` path in udev `RUN+=` actions; `Type=idle` deferral eliminates getty/PAM race conditions; on-demand unit loading enables `systemctl restart` for units outside the boot dependency graph (e.g. udev-triggered `systemd-vconsole-setup.service`); symlink-aware unit discovery handles NixOS `/etc/systemd/system/` layouts; poison-recovering lock infrastructure prevents panic cascades from poisoned `Mutex`/`RwLock` guards; external generator framework discovers generators in NixOS store paths via executable-relative search (15 generators execute successfully during boot); networkd integration enabled (`withNetworkd = true`) with `.network` file for DHCP on ethernet interfaces and D-Bus interface; resolved integration enabled (`services.resolved.enable = true`) with stub DNS on 127.0.0.53, fallback DNS servers, and D-Bus interface; portabled integration enabled (`withPortabled = true`) for portable service image management with D-Bus; timedated integration enabled (`withTimedated = true`) for time/date management with D-Bus; machined integration enabled (`withMachined = true`) for VM/container registration with D-Bus; homed integration enabled (`withHomed = true`) for home directory management with D-Bus; `networkctl persistent-storage` subcommand added for NixOS `systemd-networkd-persistent-storage.service` compatibility; deadlock-free PID table (`Arc<Mutex<PidTable>>`) allows signal handler to update entries without RuntimeInfo read lock, preventing 3-way RwLock deadlock with activation threads and control handler; deferred D-Bus registration pattern for hostnamed/timedated/localed/machined/portabled/homed/networkd/resolved/timesyncd — daemons send READY=1 immediately and connect to D-Bus in the first main-loop iteration, avoiding blocking early boot when dbus-daemon isn't ready yet; path unit watcher thread monitors `.path` units with inotify-based instant notification and poll-based safety-net fallback; shared `libsystemd::link_config` module provides `.link` file parsing used by both networkd and udevd `net_setup_link` builtin; shared `libsystemd::hwdb` module provides compiled hardware database (`hwdb.bin`) reader used by udevd `hwdb` builtin for device property lookups; cloud-hypervisor VM boots with full networking (TAP device + dnsmasq DHCP) in ~7 seconds