# Phase 2 — Essential System Services

Services required for a fully functional desktop or server:

- ✅ **`udevd`** — device manager daemon with netlink uevent monitoring (AF_NETLINK / NETLINK_KOBJECT_UEVENT), `.rules` file parser (full syntax: match/assign operators `==`/`!=`/`=`/`+=`/`-=`/`:=`, line continuation, escape sequences, `GOTO`/`LABEL` control flow), property matching (`KERNEL`, `SUBSYSTEM`, `ACTION`, `DRIVER`, `DEVTYPE`, `ATTR{file}`, `ENV{key}`, `RESULT`, `TEST`), parent device traversal (`KERNELS`, `SUBSYSTEMS`, `DRIVERS`, `ATTRS{file}`), assignment actions (`NAME`, `SYMLINK`, `OWNER`, `GROUP`, `MODE`, `ENV{key}`, `TAG`, `RUN{program}`, `RUN{builtin}`, `ATTR{file}`, `SYSCTL{key}`, `OPTIONS`), `IMPORT{program|file|cmdline|builtin|db|parent}`, `PROGRAM` execution with result capture and propagation, udev-style substitution expansion (`$kernel`/`%k`, `$number`/`%n`, `$devpath`/`%p`, `$id`/`%b`, `$driver`, `$attr{file}`/`%s{file}`, `$env{key}`/`%E{key}`, `$major`/`%M`, `$minor`/`%m`, `$result`/`%c` with index, `$name`/`%D`, `$links`, `$root`, `$sys`, `$devnode`/`%N`), glob matching (`*`, `?`, `[...]`, `|` alternatives), device database persistence (`/run/udev/data/`), **database locking** (flock-based exclusive/shared locking via `/run/udev/data/.lock` — exclusive `LOCK_EX` for writes and removals, shared `LOCK_SH` for reads during remove-event symlink recovery; lock lifetime tied to `File` handle scope via RAII drop; serialises concurrent worker threads and external readers like `udevadm info`), device tag management (`/run/udev/tags/`), symlink creation/removal in `/dev/`, device node permission setting (OWNER/GROUP/MODE with name resolution), sysfs attribute writing, RUN program execution with environment passing, builtin handlers (path_id, input_id, usb_id, net_id, blkid, kmod, **hwdb**, **keyboard**, **net_setup_link**), **`hwdb` builtin** (reads compiled hardware database `hwdb.bin` from standard search paths `/etc/systemd/hwdb/hwdb.bin`, `/etc/udev/hwdb.bin`, `/usr/lib/systemd/hwdb/hwdb.bin`, `/usr/lib/udev/hwdb.bin` plus NixOS package-relative paths; full binary trie reader with on-disk format parsing — header validation with `KSLPHHRH` signature, trie node/child/value entry deserialization, path-compressed prefix handling, v1 and v2 value entry support; trie traversal with `fnmatch`-style pattern matching — exact character walk with automatic switch to recursive pattern reconstruction and glob matching when `*`/`?`/`[` metacharacters are encountered in node prefixes or child edges; `--subsystem=SUB` parent device traversal walks sysfs hierarchy to find ancestor in requested subsystem and reads its MODALIAS; `--filter=PATTERN` glob filtering on property keys; `--lookup-prefix=PFX` prepends prefix to modalias; `--device=DEVID` alternate source device; positional modalias argument; USB modalias composition from `idVendor`/`idProduct` sysfs attributes matching systemd's `modalias_usb()`; hwdb loaded at daemon startup alongside rules and reloaded on SIGHUP; shared via `Arc<Option<Hwdb>>` with worker threads; `libsystemd::hwdb` module provides `Hwdb` struct with `open()`/`open_default()`/`from_bytes()`/`lookup()`/`get()`, `HwdbBuiltinArgs` parser, `filter_properties()`, and `fnmatch()` glob matcher; 56 unit tests in libsystemd::hwdb + 10 in udevd), **`net_setup_link` builtin** (loads `.link` files from standard search paths via shared `libsystemd::link_config` module, matches network devices against `[Match]` sections, resolves interface names via `NamePolicy=` by checking `ID_NET_NAME_FROM_DATABASE`/`ID_NET_NAME_ONBOARD`/`ID_NET_NAME_SLOT`/`ID_NET_NAME_PATH`/`ID_NET_NAME_MAC` environment variables from earlier builtins, falls back to explicit `Name=`, sets `ID_NET_LINK_FILE` to matching `.link` file path, sets `ID_NET_NAME` for interface renaming, propagates `MTUBytes` as `ID_NET_LINK_FILE_MTU`, `MACAddress` as `ID_NET_LINK_FILE_MACADDRESS`, `MACAddressPolicy` as `ID_NET_LINK_FILE_MACADDRESS_POLICY`, `AlternativeName`/`AlternativeNamesPolicy` as `ID_NET_LINK_FILE_ALTNAMES`), **`keyboard` builtin** (applies `KEYBOARD_KEY_*` scancode→keycode remappings from hwdb properties via `EVIOCSKEYCODE_V2` ioctl on evdev device nodes; comprehensive keycode name→value lookup table with 300+ KEY_*/BTN_* constants from linux/input-event-codes.h; case-insensitive name resolution with numeric decimal/hex fallback; `EVDEV_ABS_*` absolute axis override support with `EVIOCGABS`/`EVIOCSABS` ioctls for min/max/resolution/fuzz/flat fields; ABS_* axis name resolution table; scancode length auto-detection 1/2/4/8 bytes; 35 unit tests), **network interface renaming** via netlink RTM_SETLINK — after rules processing for "net" subsystem "add"/"move" events, renames the interface using `IFLA_IFNAME` if `ID_NET_NAME` (from `net_setup_link` builtin) or `NAME=` (from rules) specifies a different name; also applies MAC address (`IFLA_ADDRESS`) from `ID_NET_LINK_FILE_MACADDRESS` and MTU (`IFLA_MTU`) from `ID_NET_LINK_FILE_MTU`; self-contained NETLINK_ROUTE socket with proper bind/send/recv/ACK handling; MAC address parsing (colon-separated hex); `INTERFACE` env var updated after successful rename so downstream RUN programs and database entries see the final name; graceful error handling for missing ifindex, same-name no-op, invalid MAC format, zero MTU; 29 new unit tests), **event worker thread pool** (concurrent event processing with per-device serialization via `busy_devpaths` tracking — events for the same devpath are queued while earlier events for that device are in-flight, matching real systemd behaviour; configurable `--children-max` concurrency limit; graceful fallback on thread spawn failure), **PROGRAM result capture propagation** (`PROGRAM==` match captures stdout into `program_result`, available to subsequent rules via `$result`/`%c`/`RESULT==`; `PROGRAM=` assignment also captures; event environment passed to PROGRAM commands for full context), **inotify-based rules auto-reload** (`InotifyRulesWatcher` monitors all existing rules directories plus NixOS package-relative paths using `IN_CLOSE_WRITE`/`IN_DELETE`/`IN_MOVED_TO`/`IN_MOVED_FROM`/`IN_CREATE` events; only `.rules` file changes trigger reload; 3-second debounce timer batches rapid changes like package manager installs into a single reload, matching real systemd behaviour; graceful fallback to SIGHUP-only reload if inotify is unavailable; adaptive main-loop poll timeout accelerates during debounce; 8 unit tests), event queue with settle support and **exec queue pause/resume** (events continue to be queued while paused but no workers are dispatched, matching real systemd `StartExecQueue`/`StopExecQueue` behaviour), control socket, **D-Bus interface (`org.freedesktop.udev1`)** with full `Manager` object at `/org/freedesktop/udev1` — methods: `Ping` (liveness check), `Reload` (reload rules and hwdb), `Exit` (clean daemon shutdown), `StartExecQueue`/`StopExecQueue` (pause/resume event processing), `SetLogLevel` (runtime log level change with syslog-style level names: emerg/alert/crit/err/warning/notice/info/debug), `SetChildrenMax` (runtime worker pool sizing); properties: `ResolveNames` (name resolution mode), `Children` (active worker count), `ChildrenMax` (max workers), `ExecQueuePaused` (queue pause state); `DaemonState` shared via `Arc<Mutex<>>` between D-Bus interface and main loop for `children_max` and event queue access; graceful fallback to control-socket-only operation when D-Bus is unavailable; zbus blocking connection with background dispatch thread; 14 new unit tests), sd_notify protocol (READY/WATCHDOG/STATUS/STOPPING), signal handling (SIGTERM/SIGINT/SIGHUP/SIGCHLD); `udevadm` CLI with `info` (query by name/path, property export, attribute walk, database export, cleanup, `--device-id-of-file` for block device identification), `trigger` (action/subsystem/attribute/property/tag/sysname filters, prioritized subsystems, dry-run, verbose), `settle` (timeout, exit-if-exists, **inotify-based queue file watching** — watches `/run/udev/` for queue file deletion via inotify with 50ms poll for responsive settle detection, falls back to 200ms polling if inotify unavailable; 6 new unit tests), `monitor` (kernel uevent listening with subsystem filter, property display), `test` (device property display with rules file enumeration), `control` (reload/ping/stop/start exec queue, exit, log level, children max), `test-builtin`, `version`; 268 unit tests (229 udevd + 39 udevadm)
- ✅ **`tmpfiles`** — create/delete/clean temporary files and directories per `tmpfiles.d` configuration
- ✅ **`sysusers`** — create system users and groups per `sysusers.d` configuration
- ✅ **`logind`** — login/seat/session tracking with session create/release/activate/lock/unlock, seat management (seat0 + dynamic), user tracking, inhibitor locks (block/delay modes, stale cleanup, **pipe-based FD lifecycle** — `Inhibit` D-Bus method returns a pipe write-end FD to the caller; logind keeps the read-end and detects caller exit/close via readable/EOF on the read-end during periodic cleanup, automatically releasing stale inhibitors matching real systemd behaviour), input device monitoring for power/sleep buttons, idle hint tracking (per-session, per-seat, per-user, global), locked hint tracking, sd_notify/watchdog, control socket, `logind.conf` parsing ([Login] section with all standard keys, drop-in directory support, timespan parsing), VT switching via ioctl, AC power detection; **D-Bus interface (`org.freedesktop.login1`)** with full `Manager` object (GetSession/GetSessionByPID/GetUser/GetUserByPID/GetSeat, ListSessions/ListUsers/ListSeats/ListInhibitors, CreateSession/ReleaseSession, ActivateSession/ActivateSessionOnSeat, LockSession/UnlockSession/LockSessions/UnlockSessions, KillSession/KillUser, TerminateSession/TerminateUser/TerminateSeat, SetUserLinger, **AttachDevice** (writes udev rules to `/etc/udev/rules.d/72-seat-<seat>.rules` with `TAG+="seat"`, `ENV{ID_SEAT}="<seat>"`, `DEVPATH=="<sysfs_path>"` per device; auto-creates dynamic seats; deduplicates attachments; tracks per-seat device lists in memory), **FlushDevices** (removes all auto-generated seat attachment udev rules, clears in-memory tracking, removes empty dynamic seats that have no sessions), **PowerOff/Reboot/Halt/Suspend/Hibernate/HybridSleep/SuspendThenHibernate** with actual execution via background `systemctl` spawn (100ms delay for D-Bus reply, inhibitor + polkit pre-check), **CanPowerOff/CanReboot/CanHalt/CanSuspend/CanHibernate/CanHybridSleep/CanSuspendThenHibernate** with combined inhibitor + polkit checking, **Inhibit with pipe FD** (returns `OwnedFd` write-end per real systemd protocol), ScheduleShutdown/CancelScheduledShutdown, SetWallMessage; 30+ properties including NAutoVTs, KillUserProcesses, IdleHint, BlockInhibited, DelayInhibited, HandlePowerKey/SuspendKey/HibernateKey/LidSwitch, PreparingForShutdown/Sleep, OnExternalPower, NCurrentSessions/NCurrentInhibitors), `Session` object (25 properties: Id, User, Name, Timestamp, VTNr, Seat, TTY, Display, Remote, Service, Desktop, Scope, Leader, Type, Class, Active, State, IdleHint, LockedHint; methods: Terminate, Activate, Lock, Unlock, SetIdleHint, SetLockedHint, SetType, Kill, **TakeControl** (sets session controller identified by **real D-Bus sender unique name** extracted from message headers, single-owner enforcement, force-takeover for root, clears devices on override; **sets VT_PROCESS mode** on the session's VT when the session has a VT assigned, enabling the compositor handoff protocol), **ReleaseControl** (clears controller, closes all taken devices, **restores VT_AUTO mode** when no other controlled session shares the VT), SetBrightness, **TakeDevice** (opens `/dev/char/MAJOR:MINOR` with `O_RDWR|O_CLOEXEC|O_NOCTTY|O_NONBLOCK`, returns dup'd `OwnedFd` + active flag via `zvariant::OwnedFd`; logind retains original FD in per-session device map keyed by `(major,minor)`; re-take of same device returns dup of existing FD; requires prior `TakeControl`), **ReleaseDevice** (closes and removes device from session map), PauseDeviceComplete (logs ack for diagnostics; current implementation uses "force" type which doesn't require acknowledgement — future refinement could add cooperative "pause" type with ack tracking and timeout fallback); **PauseDevice signal** `(major, minor, type)` — emitted with types `"pause"`/`"force"`/`"gone"` when a session loses focus and its taken devices must be released; **ResumeDevice signal** `(major, minor, fd)` — emitted with a dup'd `OwnedFd` when a session gains focus and its taken devices become active again; **session switch device tracking** — `activate_session` returns `SessionSwitchInfo` describing old/new sessions and their taken device lists, marks old-session devices inactive and new-session devices active, enabling the caller to emit PauseDevice/ResumeDevice signals outside the manager lock; **VT_PROCESS-based VT switch monitoring** — signal-driven SIGUSR1/SIGUSR2 with VT_RELDISP for full compositor handoff protocol: `VtMonitor` struct opens `/dev/tty{N}` and sets `VT_PROCESS` mode via `VT_SETMODE` ioctl with `relsig=SIGUSR1`/`acqsig=SIGUSR2`; RAII drop restores `VT_AUTO` mode; `AtomicBool` signal flags (`VT_RELEASE_FLAG`/`VT_ACQUIRE_FLAG`) checked each main-loop iteration; on SIGUSR1 (VT release): `handle_vt_release()` marks all taken devices inactive, calls `VT_RELDISP(1)` to allow the switch, returns `VtSwitchAwayInfo` for PauseDevice("force") D-Bus signal emission outside the lock; on SIGUSR2 (VT acquire): `handle_vt_acquire()` reads active VT via `VT_GETSTATE` ioctl on `/dev/tty0`, finds the session by VT number, activates it, calls `VT_RELDISP(VT_ACKACQ)`, returns `VtSwitchToInfo` for ResumeDevice D-Bus signal emission with dup'd FDs; per-VT monitor lifecycle tied to TakeControl/ReleaseControl — monitors created when a session with a VT takes control, removed when control is released (only if no other controlled session shares the VT); `VtMode`/`VtStat` repr(C) structs matching kernel layout (8/6 bytes); `emit_pause_device_signal`/`emit_resume_device_signal` helpers construct raw D-Bus signal messages via `zbus::message::Message::signal()` with proper FD passing for ResumeDevice; batch helpers `emit_pause_devices`/`emit_resume_devices` for VT switch events; all VT monitors cleaned up on daemon shutdown; 12 new unit tests), `Seat` object (8 properties: Id, ActiveSession, CanGraphical, CanMultiSession, Sessions, IdleHint, IdleSinceHint; methods: Terminate, ActivateSession, SwitchTo, SwitchToNext, SwitchToPrevious), `User` object (15 properties: UID, GID, Name, Timestamp, RuntimePath, Service, Slice, Display, State, Sessions, IdleHint, Linger; methods: Terminate, Kill); D-Bus signal emission (SessionNew/SessionRemoved, UserNew/UserRemoved, SeatNew/SeatRemoved, PrepareForShutdown/PrepareForSleep, session Lock/Unlock, **PauseDevice/ResumeDevice**); dynamic object registration/unregistration for sessions, seats, users; **polkit authorization with D-Bus caller credential extraction** (`get_caller_credentials` async helper uses `zbus::message::Header` sender + `zbus::fdo::DBusProxy::get_connection_unix_user`/`get_connection_unix_process_id` to resolve the real UID and PID of every D-Bus caller; graceful fallback to uid=0/pid=0 when sender is unavailable or credential queries fail; `polkit_check` calls `pkcheck` for action authorization with full action-ID mapping for all power/sleep actions — `org.freedesktop.login1.power-off`/`.reboot`/`.halt`/`.suspend`/`.hibernate`; graceful fallback when polkit is unavailable: root gets "yes", others get "challenge"; `can_action_with_polkit` combines inhibitor lock check with polkit query using real caller credentials for `Can*` D-Bus properties; `execute_power_action` pre-checks both inhibitors and polkit with real credentials before spawning; `Inhibit` D-Bus method passes real caller UID/PID to the inhibitor record; **improved `can_action` inhibitor matching** — maps action names to canonical `what` categories: poweroff/reboot/halt→"shutdown", suspend/hibernate/hybrid-sleep/suspend-then-hibernate→"sleep", so a "shutdown" inhibitor correctly blocks all shutdown-class actions and a "sleep" inhibitor correctly blocks all sleep-class actions without cross-blocking); **multi-seat device assignment** (`attach_device` writes persistent udev rules to `/etc/udev/rules.d/72-seat-<seat>.rules` tagging devices with `TAG+="seat"` and `ENV{ID_SEAT}`, auto-creates dynamic seats for non-seat0 assignments, deduplicates, tracks per-seat device lists; `flush_attached_devices` removes all auto-generated rules, clears in-memory tracking, garbage-collects empty dynamic seats with no sessions; 8 unit tests); **ACL management** (POSIX ACL grant/revoke on seat device nodes during session activation — `apply_device_acls` resolves sysfs paths to `/dev/` nodes via `dev` file major:minor or `uevent` DEVNAME and grants `user:UID:rw` via `setfacl`; `remove_device_acls` revokes the per-user entry; integrated into `activate_session` so old user's ACLs are revoked and new user's ACLs are applied on every session switch; graceful fallback when `setfacl` is unavailable; `devnode_from_sysfs` helper reads sysfs `dev` and `uevent` files; 5 unit tests); **automatic session detection on startup** (`detect_sessions_from_runtime` re-enumerates `/run/systemd/sessions/` state files to recover sessions that existed before a daemon restart — parses KEY=VALUE format, validates leader PID is still alive, removes stale session files, bumps `next_session_id` past recovered IDs; `detect_sessions_from_utmp` parses `/var/run/utmp` binary records (384-byte glibc x86_64 format) to find `USER_PROCESS` (type=7) entries — reads `ut_type`, `ut_pid`, `ut_line`, `ut_user`, `ut_host` fields, validates PID liveness, resolves username→UID, determines session type from TTY name (tty→"tty", `:*`→"x11"), assigns VT number from ttyN, sets remote flag from ut_host; `parse_env_file` KEY=VALUE parser and `read_cstr_field` NUL-terminated C string reader; `resolve_username_to_uid` via `/etc/passwd`; 8 unit tests); `loginctl` CLI with list/show/activate/lock/terminate commands; 167 unit tests (logind) + 12 unit tests (loginctl)
- ✅ **`pam_systemd`** — PAM module (`pam_systemd.so`) for automatic logind session management on login/logout; built as a `cdylib` crate exporting standard C PAM entry points (`pam_sm_open_session`, `pam_sm_close_session`, `pam_sm_acct_mgmt`, `pam_sm_setcred`, `pam_sm_authenticate`, `pam_sm_chauthtok`); **session creation** on `pam_sm_open_session`: resolves PAM user→UID via `getpwnam`, determines session type from `$XDG_SESSION_TYPE` / module `type=` argument / heuristic (`$WAYLAND_DISPLAY`→wayland, `$DISPLAY`→x11, default→tty), determines session class from `$XDG_SESSION_CLASS` / module `class=` argument (default "user"), reads TTY from `PAM_TTY`, extracts VT number from ttyN name (1–63 range), reads `$XDG_SEAT` / `$XDG_SESSION_DESKTOP` / `$DISPLAY`, gets leader PID from `getpid()`, reads `PAM_SERVICE` and `PAM_RHOST` for service name and remote host; communicates with logind via Unix control socket at `/run/systemd/logind-control` sending JSON `create-session` command; on success sets `XDG_SESSION_ID`, `XDG_RUNTIME_DIR`, `XDG_SESSION_TYPE`, `XDG_SESSION_CLASS`, `XDG_SEAT`, `XDG_VTNR`, `XDG_SESSION_DESKTOP` in the PAM environment; ensures `/run/user/<uid>` exists with mode 0700 and correct ownership; returns `PAM_IGNORE` on failure (graceful degradation matching real pam_systemd when logind is down); **session release** on `pam_sm_close_session`: reads `$XDG_SESSION_ID` from PAM env and sends `release-session` to logind; `debug` module argument enables verbose syslog logging; PAM FFI via `unsafe extern "C"` block with `pam_get_item`/`pam_get_user`/`pam_getenv`/`pam_putenv`; JSON escaping helper for safe command serialization; module argument parser supporting `key=value` and flag-only forms; 22 unit tests
- ✅ **`user-sessions`** — manage `/run/nologin` to permit/deny user logins during boot/shutdown
- ✅ **`update-done`** — create/update `/etc/.updated` and `/var/.updated` stamp files for `ConditionNeedsUpdate=`
- ✅ **`random-seed`** — load/save kernel random seed across reboots via `/var/lib/systemd/random-seed`
- ✅ **`pstore`** — archive `/sys/fs/pstore/` crash entries to `/var/lib/systemd/pstore/`
- ✅ **`machine-id-setup`** — initialize or commit `/etc/machine-id` (with `--commit`, `--print`, `--root`)
- ✅ **`modules-load`** — load kernel modules from `modules-load.d` configuration
- ✅ **`sysctl`** — apply sysctl settings from `sysctl.d` configuration
- ✅ **`binfmt`** — register binary formats via `binfmt_misc` from `binfmt.d` configuration
- ✅ **`vconsole-setup`** — virtual console font and keymap configuration
- ✅ **`backlight`** / ✅ **`rfkill`** — save and restore hardware state across reboots
- ✅ **`ask-password`** / ✅ **`tty-ask-password-agent`** — password query framework with TTY input (echo suppression, backspace handling), agent protocol via question files in `/run/systemd/ask-password/`, Unix socket reply (`+password` / `-` cancel), credential lookup, wall message broadcasting, continuous watch mode; missing: inotify-based watching (uses polling), Plymouth integration, kernel keyring caching