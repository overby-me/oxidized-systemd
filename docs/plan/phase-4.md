# Phase 4 — Extended Services

Higher-level management capabilities:

- ✅ **`machined`** — VM and container registration/tracking daemon with machine registry (register/terminate/GC), machine class (VM/container), state tracking (opening/running/closing), runtime state files in `/run/systemd/machines/`, control socket at `/run/systemd/machined-control`, stale machine cleanup (leader PID liveness check), sd_notify protocol (READY/WATCHDOG/STATUS/STOPPING), signal handling (SIGTERM/SIGINT for shutdown, SIGHUP for reload), periodic GC of dead machines; **D-Bus interface (`org.freedesktop.machine1`)** with Manager object (ListMachines, GetMachine, GetMachineByPID, RegisterMachine, TerminateMachine, KillMachine, GetMachineOSRelease, ListImages, CloneImage, RenameImage, RemoveImage, SetImageLimit, SetPoolLimit, GetImageOSRelease, CopyTo, CopyFrom, BindMount, BindUser, OpenMachineLogin, OpenMachineShell, ImportTar, ImportRaw, ExportTar, ExportRaw, PullTar, PullRaw, CleanPool, Describe; properties: PoolPath, PoolUsage, PoolLimit with real statvfs-based stats); deferred D-Bus registration (connects after READY=1 to avoid blocking early boot); **image management** — `ImageInfo` data model with type (directory/raw), path, read_only, crtime/mtime, usage, limit; image discovery from `/var/lib/machines/` (sorted, hidden-file skipping); `clone_image` (recursive `cp -a` for directories, `fs::copy` for raw, `--read-only` support); `rename_image` (with `.limit` sidecar migration); `remove_image` (directory `remove_dir_all` / file `remove_file` + limit cleanup); `set_image_limit` (`.limit` sidecar file, zero removes); `find_image` (exact name + `.raw` extension fallback); `format_bytes`/`parse_size` (B/K/M/G/T suffixes); **machine scoping** — `create_machine_scope()` creates transient `machine-<name>.scope` unit via `org.freedesktop.systemd1.Manager.StartTransientUnit` D-Bus call with Description/PIDs/Delegate properties; `stop_machine_scope()` via `StopUnit`; scope auto-created during RegisterMachine and REGISTER control command; scope auto-stopped on terminate; **copy-to/copy-from** — `copy_to_machine`/`copy_from_machine` copy files between host and running containers by resolving paths through the machine's `root_directory`; supports both files (fs::copy) and directories (recursive `cp -a`); COPY-TO/COPY-FROM control socket commands; CopyTo/CopyFrom D-Bus methods; **bind mounts** — `bind_mount_in_machine()` bind-mounts a host path into a running container's mount namespace via `fork()` + `setns(CLONE_NEWNS)` into `/proc/<leader>/ns/mnt`, then `mount()` with `MS_BIND|MS_REC`; `--read-only` remounts with `MS_RDONLY`; `--mkdir` creates destination directory before mounting; `bind_user_in_machine()` resolves user home directory from container's `/etc/passwd` (by name or numeric UID) and bind-mounts into a user-accessible path; `resolve_container_user_home()` parses `/etc/passwd` with comment/blank line skipping, falls back to `/root` for root/UID 0 or `/home/<user>` for unknown users; BIND/BIND-MOUNT/BIND-USER/BIND_USER control socket commands; BindMount/BindUser D-Bus methods; `machinectl bind NAME SRC [DEST]` with `--read-only`, `--mkdir`, and `--uid=USER` (for bind-user mode); **PTY forwarding** — `open_machine_pty()` allocates PTY pair via `openpty()`, forks child that enters container's PID/mount/UTS/IPC/net namespaces via `/proc/<leader>/ns/*` + `setns()`, sets up slave PTY as stdin/stdout/stderr with `setsid()`/`TIOCSCTTY`, chroots into container root, optionally switches user, execs `/bin/login` (for login) or specified shell with `-l` (for shell); LOGIN/SHELL control socket commands return master_fd + child_pid; OpenMachineLogin/OpenMachineShell D-Bus methods; **OS image import/export/pull** — `import_tar` (extract tar archive into pool directory, stdin support via `-`), `import_raw` (copy or stdin-pipe raw disk image with `.raw` extension), `export_tar` (create tar from directory image, stdout support), `export_raw` (copy raw image, stdout support), `pull_tar` (download via curl + extract), `pull_raw` (download via curl); `--read-only` for imports; `--verify` parameter (reserved for signature verification); IMPORT-TAR/IMPORT-RAW/EXPORT-TAR/EXPORT-RAW/PULL-TAR/PULL-RAW control socket commands; D-Bus ImportTar/ImportRaw/ExportTar/ExportRaw/PullTar/PullRaw methods; **pool statistics** — `pool_usage_stats()` via `statvfs` for real PoolUsage/PoolLimit D-Bus properties and Describe JSON output; `machinectl` CLI with `list` (registered machines table with class/service/state), `status` (detailed machine info with name/class/service/scope/leader/root/state/since/netif), `show` (key=value properties with `-p`/`--property` filtering and `--value` output), `terminate`/`poweroff`/`reboot` (unregister + SIGTERM leader + scope stop), `kill` (send signal to leader with `--signal`/`-s`, numeric or named signals), `clean` (trigger GC + remove `.tmp` files from pool), `list-images` (daemon-first with offline fallback, shows NAME/TYPE/RO/USAGE/LIMIT), `show-image` (detailed image info via IMAGE_SHOW), `clone` (clone image with `--read-only`), `rename` (rename image), `remove` (delete image), `set-limit` (set image size limit with K/M/G/T suffix), `copy-to` (copy host→container files), `copy-from` (copy container→host files), `bind` (bind-mount host path into container with `--read-only`/`--mkdir`, `--uid=USER` for bind-user mode), `login` (PTY-forwarded login with raw terminal mode, poll-based I/O forwarding, terminal restore on exit), `shell` (PTY-forwarded shell with `USER@NAME` syntax, `--uid` option, optional command arguments), `import-tar`/`import-raw` (import images with `--read-only`), `export-tar`/`export-raw` (export images, stdout default), `pull-tar`/`pull-raw` (download+import with `--verify` option); `image` subcommand aliases (`image show`/`clone`/`rename`/`remove`/`set-limit`); `pty_forward()` with raw terminal mode (`cfmakeraw`), `poll()`-based bidirectional I/O, child exit detection via `waitpid(WNOHANG)`, output draining, terminal restoration; offline fallback reads state files directly when daemon is unavailable; 297 unit tests (199 machined + 98 machinectl) covering machine class/state parsing and display, state file roundtrips (with/without netif, VM/container classes, minimal fields, missing/invalid fields), machine format (status/show output), name validation (valid names, .host special name, length limits, invalid chars), registry operations (register/terminate/get/find-by-leader/duplicate/empty-name/invalid-name), persistence (save/load/save-one, empty/nonexistent dirs, dotfile skipping, invalid file skipping), GC (keeps alive PIDs, removes dead, leader-zero), format_list (empty/with-machines), control commands (PING/LIST/STATUS/SHOW/REGISTER/TERMINATE/GC/IMAGE_LIST/IMAGE_SHOW/CLONE/RENAME/REMOVE/SET-LIMIT/COPY-TO/COPY-FROM/BIND/BIND-MOUNT/BIND-USER/BIND_USER/LOGIN/SHELL/IMPORT-TAR/IMPORT-RAW/EXPORT-TAR/EXPORT-RAW/PULL-TAR/PULL-RAW, case insensitivity, error cases, missing args), D-Bus registration, object path encoding, JSON escaping, env content parsing, timestamp formatting, **image type parsing** (directory/subvolume/raw/invalid/display/as_str), **image discovery** (empty dir/nonexistent/directory images/raw images/hidden skipping/sorted), **find_image** (exact name/raw extension fallback/not found), **clone_image** (directory clone/already-exists/not-found/invalid-dest-name), **rename_image** (directory/not-found/dest-exists/limit-file migration), **remove_image** (directory/raw/not-found/limit-file cleanup), **set_image_limit** (creates limit file/zero removes/not-found), **ImageInfo format** (status/show output, from_path directory/raw/hidden/with-limit-file), **format_bytes** (zero/bytes/K/M/G/mixed), **parse_size** (bytes/K/M/G/T/empty/invalid), **copy-to/copy-from** (machine-not-found/source-not-found/file-roundtrip), **bind mounts** (machine-not-found/no-leader/source-not-found for bind and bind-user, control command BIND/BIND-MOUNT/BIND-USER/BIND_USER missing-args/not-found/source-not-found/case-insensitive, D-Bus method struct presence), **resolve_container_user_home** (root by name/by UID, user by name/by UID, missing passwd, user-not-found defaults, root-UID defaults, comments/blanks), **PTY forwarding** (not-found/no-leader error paths), **machine scoping** (scope name format/special chars), **pool_usage_stats** (nonexistent/tmp), **import** (invalid-name/already-exists), **export** (not-found/wrong-type for tar/raw), **pull** (invalid-name/already-exists), argument parsing (all commands/flags/options including clone/rename/remove/set-limit/copy-to/copy-from/bind/bind-read-only/bind-mkdir/bind-no-dest/bind-with-uid/import-tar/import-raw/export-tar/export-raw/pull-tar/pull-raw/show-image/shell with USER@/--uid/--read-only/--mkdir/--verify, image subcommand aliases), signal parsing (numeric/named/case-insensitive/unknown-fallback), run integration (help/unknown/login-no-daemon/shell-no-daemon/bind-no-machine/bind-no-source/bind-no-daemon/bind-user-no-daemon/missing-args for all new commands)
- ✅ **`nspawn`** — lightweight namespace container runtime with `fork()` + `unshare()` based container creation (PID, mount, UTS, IPC namespaces), `--boot` mode (find and exec init inside container), `--directory=PATH` container root, `--machine=NAME` with auto-derivation from path, `--bind=SRC[:DST[:OPTS]]` / `--bind-ro` bind mounts (recursive by default, `norbind` option), `--tmpfs=PATH[:OPTS]` tmpfs mounts, `--overlay` / `--overlay-ro` overlay mounts, `--private-network` (CLONE_NEWNET), `--network-veth` / `-n` (**virtual ethernet pair creation** via netlink — creates a veth pair with host-side `ve-<machine>` and container-side `host0` matching real systemd-nspawn naming; `RTM_NEWLINK` with `IFLA_LINKINFO`/`IFLA_INFO_KIND="veth"`/`IFLA_INFO_DATA`/`VETH_INFO_PEER` nested attributes for pair creation; `RTM_SETLINK` with `IFLA_NET_NS_PID` to move container end into child's network namespace; `RTM_SETLINK` with `IFF_UP` to bring up host-side interface; host-side name truncated to IFNAMSIZ-1 (15 chars); self-contained `NETLINK_ROUTE` socket with proper bind/send/recv/ACK handling; `read_ifindex()` via sysfs for interface index resolution; dual sync pipe architecture — `child_ready_pipe` for child→parent signaling after `unshare()` completes, `parent_done_pipe` for parent→child signaling after veth/uid setup, ensuring the child's network namespace exists before veth pair creation; graceful fallback on veth failure — container still runs with isolated empty network), `--private-users` / `-U` (CLONE_NEWUSER with uid_map/gid_map/setgroups setup), `--capability=CAP` / `--drop-capability=CAP` (41 Linux capabilities with CAP_ prefix handling, `all` keyword, comma-separated lists; default set of 23 capabilities matching systemd-nspawn; bounding set enforcement via `prctl(PR_CAPBSET_DROP)`), `--as-pid2` stub init mode, `--user=USER` (numeric UID or name lookup via getpwnam), `--chdir=PATH`, `--setenv=VAR=VALUE` / `-E` (with host inheritance), `--volatile[=yes|state|overlay|no]`, `--resolv-conf=MODE` (15 modes: off/copy-host/copy-stub/copy-uplink/bind-host/bind-stub/replace-*/delete/auto), `--timezone=MODE` (off/copy/bind/symlink/delete/auto), `--link-journal=MODE` (no/host/try-host/guest/try-guest/auto), `--console=MODE` (interactive/read-only/passive/pipe/autopipe), `--port=PROTO:HOST:CONTAINER` / `-p` (TCP/UDP port forwarding parsing), `--hostname=NAME`, `--uuid=UUID`, `--register=BOOL` (machined registration via control socket), `--keep-unit`, `--no-new-privileges` (`PR_SET_NO_NEW_PRIVS`), `--read-only`, `--notify-ready`, `--kill-signal=SIG`, `--settings=BOOL`, `--property=NAME=VALUE` (scope properties), `--quiet` / `-q`; essential API filesystem setup inside container (`/proc`, `/sys` read-only bind, `/dev` tmpfs with bind-mounted device nodes null/zero/full/random/urandom/tty + symlinks fd/stdin/stdout/stderr/ptmx, `/dev/pts` devpts with newinstance, `/dev/shm` tmpfs, `/dev/mqueue`, `/run` tmpfs, `/tmp` tmpfs), console/TTY passthrough (`/dev/console` bind from host TTY), `pivot_root` with `chroot` fallback, clean environment setup (`container=systemd-nspawn`, `container_uuid`, `PATH`), init discovery (`usr/lib/systemd/systemd`, `lib/systemd/systemd`, `sbin/init`, etc.), shell discovery (bash/sh/zsh), user switching (uid/gid via `setuid`/`setgid`), parent-child synchronization via dual pipe pairs (child_ready + parent_done), `waitpid` with signal forwarding, machine name sanitization (alphanumeric/dot/underscore/dash, max 64 chars), machined registration/unregistration via JSON control socket protocol; **seccomp profiles** — `--seccomp-profile=MODE` (default/strict/allow-all/PATH) with BPF-based syscall filtering via `prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER)`; `build_seccomp_filter()` generates BPF program with LD+JEQ+RET chain; default deny list blocks dangerous syscalls (kexec_load, kexec_file_load, init_module, finit_module, delete_module, ioperm, iopl, swapon, swapoff, open_by_handle_at, name_to_handle_at, keyctl, add_key, request_key, uselib, acct, nfsservctl); strict mode adds mount/umount2/ptrace/personality/reboot/chroot/process_vm_readv/process_vm_writev; custom mode loads raw BPF from file; `seccomp_deny_list()` collects denied syscalls per profile; NO_NEW_PRIVS auto-set before filter installation; x86_64 syscall numbers with non-x86_64 fallback; **`--image` disk image support** — `setup_loopback()` via `/dev/loop-control` LOOP_CTL_GET_FREE ioctl + LOOP_SET_FD + LOOP_SET_STATUS64 with LO_FLAGS_AUTOCLEAR|LO_FLAGS_PARTSCAN|LO_FLAGS_READ_ONLY; `detach_loopback()` via LOOP_CLR_FD; GPT dissection via `dissect_gpt()` reading GPT header at LBA 1 (signature verification, partition entry parsing with type GUID/first_lba/last_lba); `DissectedPartition` with size_bytes/is_root/is_usr methods; well-known GPT type GUIDs (root-x86_64, root-aarch64, usr-x86_64, linux-generic); `find_root_partition()` preferring architecture-specific root over generic; `resolve_image_root()` orchestrates loopback→dissect→mount pipeline; `mount_device()` tries ext4/btrfs/xfs/vfat/erofs/squashfs; `mount_device_with_offset()` for partition-level mounting when device nodes unavailable; `create_image_mount_dir()` in `/run/systemd/nspawn/`; `cleanup_image_mount()` unmounts and removes temp dir; also resolves `/var/lib/machines/<name>.raw` images by machine name; **OCI bundle runtime** — `--oci-bundle=PATH` reads OCI runtime spec `config.json`; `OciConfig` struct with root_path/root_readonly/args/env/cwd/hostname/mounts/namespaces; `OciMount` struct with destination/source/mount_type/options; `parse_oci_config()` with minimal JSON parser; `extract_json_string()` for key-value extraction; `extract_json_string_array()` with proper string/escape handling; `parse_oci_mounts()` with bracket-depth tracking for nested arrays; `resolve_oci_root()` validates bundle directory and rootfs existence; **network configuration inside container** — `--network-config=MODE` (none/dhcp/ADDR/PREFIX[:GATEWAY]); `NetworkConfig` enum with None/Dhcp/Static variants; `setup_container_network()` runs inside container namespace; static IP via netlink `RTM_NEWADDR` with `IFA_LOCAL`/`IFA_ADDRESS` attributes; default route via `RTM_NEWROUTE` with `RTA_GATEWAY`/`RTA_OIF`; `parse_ipv4()` address parser; `vec_put_rta_bytes()`/`vec_put_rta_u32()` Vec-based netlink attribute helpers with alignment; `add_ipv4_address()` and `add_ipv4_default_route()` construct full netlink messages; DHCP mode via `run_dhcp_client()` trying dhclient/dhcpcd/udhcpc with fork+exec+waitpid; auto brings up host0 before configuration; **cgroup delegation** — `--cgroup-delegate` creates cgroup v2 subtree `machine-<name>` under current cgroup; `setup_cgroup_delegation()` reads `/proc/self/cgroup` for v2 path, creates child cgroup, writes container PID to `cgroup.procs`, enables cpu/memory/io/pids controllers in both parent `subtree_control` and container `subtree_control` for full delegation; `cleanup_cgroup()` removes the container cgroup directory on exit; `read_self_cgroup()` parses cgroup v2 unified hierarchy path; **`.nspawn` settings file parsing** — `NspawnSettings` struct with [Exec] (Boot, ProcessTwo, Parameters, Environment, User, WorkingDirectory, Capability, DropCapability, KillSignal, Hostname, MachineId, NoNewPrivileges, PrivateUsers, NotifyReady), [Files] (ReadOnly, Volatile, Bind, BindReadOnly, Tmpfs, Overlay, OverlayReadOnly), and [Network] (Private, VirtualEthernet, Port, NetworkConfig) sections; `parse_nspawn_settings()` / `parse_nspawn_settings_str()` INI parser with comment/blank line skipping, case-insensitive keys, both camelCase and hyphen-separated key variants; `find_nspawn_settings()` searches `/etc/systemd/nspawn/`, `/run/systemd/nspawn/`, `/var/lib/machines/`, and alongside the image file; `apply_nspawn_settings()` merges settings as defaults (CLI args override) for all supported fields including bind mounts, capabilities, environment, port forwards, volatile mode, network config; `parse_bool_setting()` supports yes/no/true/false/1/0/on/off; auto-loaded on startup unless `--settings=no`; 251 unit tests covering capability parsing (from_str with/without CAP_ prefix, case-insensitive, all known caps, unknown, as_str, number ordering, display, list parsing single/multiple/all/empty-parts/unknown/spaces), bind mount parsing (simple/dest/options/read-only/empty-dest/empty-source/rbind-default/explicit-rbind/norbind), port forward parsing (single/host-container/full/udp/proto-only/invalid/too-many), volatile mode (all modes + invalid), resolv-conf mode (all modes + invalid), timezone mode (all modes + invalid), link-journal mode (all modes + empty + invalid), console mode (all modes + invalid), machine name (clean/special-chars/leading-trailing-dash/empty/long), machine name derivation (explicit/from-directory/from-image), capability computation (default/add-extra/drop/add-and-drop/add-duplicate), argument parsing (empty/help/short-help/version, directory long/short/attached/missing, image long/short, machine long/short, boot/boot-long, as-pid2, user long/short, chdir, setenv long/short/multiple, bind/bind-ro/multiple-binds, tmpfs/tmpfs-with-options, private-network/network-veth/network-veth-long, private-users/long/pick, capability/multiple/drop, no-new-privileges, volatile default/state/no, resolv-conf/timezone/link-journal/console, port short/long, uuid/hostname, register yes/no, keep-unit, quiet/long, read-only, property/invalid, kill-signal/settings/oci-bundle, command-after-dashdash/positional, combined full-boot-scenario/minimal-command, overlay/overlay-ro, notify-ready, seccomp-profile default/strict/allow-all/custom, network-config none/dhcp/static/static-no-gw, cgroup-delegate/yes/no, default new fields), defaults, resolve-root (no-dir/nonexistent/existing/file-not-dir/image-nonexistent/oci-nonexistent), find-init/find-shell, ensure-dir, sync-pipe, mode roundtrips, **veth tests** (host name derivation short/exact-limit/truncated/long/single-char/empty/dots-dashes/boundary-lengths, container name constant, netlink message structure/contains-host-name/contains-container-name/contains-veth-kind/contains-linkinfo/different-names/peer-ifinfomsg-present, nl_align/nl_rta_align values, nl_put_u16/u32/i32/rta_bytes/rta_u32 serialization, netlink constants, IFNAMSIZ, read_ifindex nonexistent/lo, bring_interface_up/move_interface_to_ns graceful failure, network_veth implies CLONE_NEWNET, parse_args network_veth flag), **seccomp tests** (profile from_str default/strict/allow-all/off/custom path, as_str all modes, deny list default non-empty/strict superset/allow-all empty/custom empty, build_seccomp_filter empty/single-deny/multiple-deny structure verification, BPF constants, apply_seccomp_filter allow-all ok/custom nonexistent error), **disk image tests** (LoopInfo64 default, loop constants, setup_loopback nonexistent/not-file, detach_loopback nonexistent, DissectedPartition size_bytes/is_root/is_usr, GPT type GUIDs 16 bytes, IMAGE_SECTOR_SIZE/IMAGE_GPT_SIGNATURE constants, find_root_partition prefers-specific/falls-back-to-generic/none, dissect_gpt nonexistent/no-signature/valid-header-no-partitions/with-partition, create_image_mount_dir name extraction), **OCI bundle tests** (extract_json_string basic/nested, extract_json_string_array basic/empty/no-bracket, parse_oci_config basic/missing/defaults, resolve_oci_root valid/missing-rootfs/not-dir, parse_oci_mounts basic with nested options arrays/empty), **network config tests** (NetworkConfig from_str none/off/dhcp/static-with-gw/static-no-gw/invalid, as_str all modes, parse_ipv4 valid/invalid, read_self_cgroup, cgroup path format), **`.nspawn` settings tests** (parse empty/exec-section full/files-section full/network-section full, comments and blanks, case-insensitive keys, environment/parameters/multiple-binds/unknown-section/hyphenated-keys/network-key-variants/files-key-variants, parse_bool_setting all values, find_nspawn_settings nonexistent/alongside-image, parse file ok/nonexistent, apply_nspawn_settings basic/cli-overrides/binds/capabilities/environment/network-config/volatile/port-forwards/parameters/parameters-not-overridden)
- ✅ **`portabled`** — portable service image management daemon with image discovery from standard search paths (`/var/lib/portables/`, `/etc/portables/`, `/usr/lib/portables/`, `/run/portables/`), image inspection (enumerate unit files, read os-release), attach/detach operations with symlink management in `/etc/systemd/system.attached/` (persistent) or `/run/systemd/system.attached/` (runtime), profile-based drop-in generation for security hardening, reattach (atomic detach + attach), attachment state tracking via marker files in `/run/systemd/portabled/`, runtime vs persistent attachment modes, control socket at `/run/systemd/portabled-control`, sd_notify protocol (READY/WATCHDOG/STATUS/STOPPING), signal handling (SIGTERM/SIGINT for shutdown, SIGHUP for reload), periodic GC of stale attachments; **D-Bus interface (`org.freedesktop.portable1`)** with Manager object (ListImages, GetImage, GetImageState, AttachImage, DetachImage, ReattachImage, RemoveImage, GetImageOSRelease, GetImageMetadata; properties: PoolPath, PoolUsage, PoolLimit); deferred D-Bus registration (connects after READY=1 to avoid blocking early boot); `portablectl` CLI with `list` (available images table with name/type/state/OS/path), `attach IMAGE [PROFILE]` (symlink units with optional profile drop-ins, `--runtime` for volatile), `detach IMAGE` (remove symlinks and drop-ins), `reattach IMAGE [PROFILE]` (atomic detach + attach), `inspect IMAGE` (show image details, os-release, unit files), `is-attached IMAGE` (check attachment state with exit code), `read-only` (stub), `set-limit` (stub); offline fallback reads state files and discovers images directly when daemon is unavailable; supports `--runtime`, `--no-reload`, `--no-pager`, `--no-legend`, `--no-ask-password`, `-q`/`--quiet`, `-H`/`--host`, `-M`/`--machine`, `--json` flags; NixOS integration enabled (`withPortabled = true`); 155 unit tests (95 portabled + 60 portablectl) covering image type/attach state parsing and display, os-release reading (comments, blanks, quoted values, etc/usr fallback), unit file discovery (service/socket/target/timer/path types, deduplication, sorting), image format (status/show output), attachment info state file roundtrips (with/without profile, runtime flag, empty units, missing fields, comments), image registry discovery (directory/raw images, hidden file skipping, priority ordering, nonexistent dirs, multiple images), attachment persistence (save/load/single, empty/nonexistent dirs, dotfile/invalid skipping, file removal), attach/detach operations (basic, runtime, already-attached, not-found, raw-not-supported, no-units, multiple-units, marker drop-ins), GC (removes stale, keeps live, empty), image list formatting, inspect (directory/raw/not-found), profile resolution (dir-style, file-style, not-found, listing), control commands (PING, LIST, INSPECT, ATTACH, DETACH, REATTACH, IS-ATTACHED, SHOW, STATUS, GC, RELOAD, case insensitivity, missing args, unknown commands), D-Bus registration, object path encoding, attach/detach/reattach cycles, helper functions (format_bytes, timestamps, days_to_ymd); portablectl parse_command tests (all commands, all flags, missing args, unknown commands, flag stripping, host/machine/json flag skipping), offline discovery (directory/raw/hidden/multiple/priority/nonexistent), offline unit discovery, offline os-release reading, offline attach state, offline inspect, command format verification; missing: raw disk image support (loopback mount, GPT dissection), extension images (`--extension`), image size limit management, automatic daemon-reload after attach/detach, read-only flag toggling
- ✅ **`homed`** — home directory management daemon with JSON-based user records (`/var/lib/systemd/home/*.identity`), storage backends (directory fully implemented, subvolume stub, luks/cifs/fscrypt detected), user lifecycle operations (create/remove/activate/deactivate/lock/unlock/update/passwd/resize), UID allocation (60001–60513 range), home area GC, control socket at `/run/systemd/homed-control`, sd_notify protocol (READY/WATCHDOG/STATUS/STOPPING), signal handling (SIGTERM/SIGINT for shutdown, SIGHUP for reload), periodic GC; **D-Bus interface (`org.freedesktop.home1`)** with Manager object (ListHomes, GetHomeByName, GetHomeByUID, ActivateHome, DeactivateHome, LockHome, UnlockHome, LockAllHomes, DeactivateAllHomes, RemoveHome, Describe; properties: AutoLogin); deferred D-Bus registration (connects after READY=1 to avoid blocking early boot); `homectl` CLI with `list`, `inspect`, `show`, `create` (with `--real-name`/`--shell`/`--storage`/`--password`/`--home-dir`/`--image-path`), `remove`, `activate`, `deactivate`, `update`, `passwd`, `resize` (K/M/G/T suffixes), `lock`, `unlock`, `lock-all`, `deactivate-all`, `with USER [-- CMD...]` (activate→run→deactivate); offline fallback reads identity files directly; NixOS integration enabled (`withHomed = true`); 183 unit tests (113 homed + 70 homectl); missing: LUKS2 encrypted images, CIFS/fscrypt/btrfs backends, PKCS#11/FIDO2 authentication, password quality enforcement, automatic activation on login, recovery keys
- ✅ **`oomd`** — userspace OOM killer with PSI-based memory pressure monitoring, `oomd.conf` parsing, managed cgroup discovery from unit files, swap usage monitoring, `oomctl` CLI with `dump` command
- ✅ **`coredump`** — core dump handler with `coredump.conf` parsing ([Coredump] section with Storage, Compress, ProcessSizeMax, ExternalSizeMax, MaxUse, KeepFree), drop-in directory support, kernel pipe handler (`/proc/sys/kernel/core_pattern` protocol with PID/UID/GID/SIGNAL/TIMESTAMP/RLIMIT/HOSTNAME/COMM/EXE arguments and `--backtrace` flag), core dump storage in `/var/lib/systemd/coredump/` with descriptive filenames (`core.COMM.UID.BOOT_ID.PID.TIMESTAMP`), JSON metadata sidecar files, size limit enforcement (ProcessSizeMax, ExternalSizeMax), automatic vacuum of old core dumps (MaxUse, KeepFree with statvfs-based free space detection), boot ID and machine ID collection, signal name mapping; `coredumpctl` CLI with `list` (tabular display with TIME/PID/UID/GID/SIG/COREFILE/EXE columns, `--lines`/`-n` limit, `--reverse`, `--since`/`--until` time filters, `--no-legend`), `info` (detailed per-dump display with username/group resolution from `/etc/passwd`/`/etc/group`), `dump` (binary output to file via `-o` or stdout with TTY safety check), `debug`/`gdb` (launch debugger with `--debugger`/`--debugger-arguments` options), match patterns (PID number, COMM name prefix, EXE path prefix); 82 unit tests covering config parsing (all Storage modes, Compress, size directives, infinity, drop-in override, case-insensitive sections, comments, missing files), size parsing (bytes/K/M/G/T/infinity), bool parsing, JSON roundtrips (basic, special characters, control chars, unescape, invalid input), metadata (signal names, filename generation, special char sanitization), argument parsing (basic, --backtrace, no exe, missing args, invalid PID), storage (basic store+read, exceeds external max, directory creation, empty data), vacuum (removes oldest, empty dir, nonexistent dir), listing (empty, with entries, skips non-core files), discovery+filter integration (by comm, PID, exe path, time range), timestamp formatting (epoch, known date, leap year), date math (days_to_ymd, is_leap_year); missing: compression (lz4/zstd/xz), journal integration, `/proc/PID/` metadata enrichment (cmdline, cgroup, environ)
- ✅ **`cryptsetup`** — LUKS/dm-crypt encrypted block device setup via device-mapper ioctl interface; `attach VOLUME DEVICE [PASSWORD [OPTIONS]]` sets up dm-crypt with DM_DEV_CREATE → DM_TABLE_LOAD → DM_DEV_SUSPEND (resume) sequence; `detach VOLUME` tears down with DM_DEV_SUSPEND → DM_TABLE_CLEAR → DM_DEV_REMOVE; full option parsing: `plain`/`luks`/`tcrypt` mode selection, `cipher=` (default aes-xts-plain64), `size=` (key size in bits, default 256), `hash=` (default sha256), `offset=`/`skip=` (sector offsets), `readonly`, `discard`/`allow-discards`, `same-cpu-crypt`, `submit-from-crypt-cpus`, `no-read-workqueue`/`no-write-workqueue`, `sector-size=` (default 512), `tries=`, `keyfile-size=`/`keyfile-offset=`, `timeout=`, `tmp=FSTYPE` (post-setup mkfs), `header=PATH` (detached LUKS header), `noauto`/`nofail`/`swap`/`x-systemd.*` compat ignored; key file reading with offset/size support and stdin fallback; LUKS v1/v2 header auto-detection; dm-crypt target table construction with feature flags; proper cleanup on failure (remove DM device if table load fails); 108 unit tests covering argument parsing (empty/help/version/attach minimal/password/options/missing args/detach/unknown), option parsing (empty/dash/none/all modes/cipher/size/hash/offset/skip/readonly/discard aliases/workqueue flags/sector-size/tries/keyfile-size/keyfile-offset/timeout/tmp/header/combined/noauto-nofail/systemd-extensions/unknown/whitespace/empty-parts/swap/auto/mode-override), key handling (hex encoding empty/single/multi/zeros/all-ff, file read/offset/size/offset+size/nonexistent/empty/offset-past-end/size-larger/binary), crypt params construction (default/discard/multiple-features/empty-key/custom-cipher/offset-skip/sector-size/all-features), DM ioctl helpers (nr encoding/init basic/readonly/suspend/combined flags/empty name/long name truncated/u32 u64 roundtrip/string write/truncation/empty/align8), target append (basic/params-content/alignment), LUKS detection (v1/v2/not-luks/empty/short/nonexistent), defaults, integration (missing device/plain empty key/detach nonexistent), full-flow (LUKS with header/plain with swap), constants
- ✅ **`veritysetup`** — dm-verity integrity-verified block device setup via device-mapper ioctl interface; `attach VOLUME DATA_DEVICE HASH_DEVICE ROOT_HASH [OPTIONS]` sets up dm-verity with DM_DEV_CREATE → DM_TABLE_LOAD → DM_DEV_SUSPEND (resume), always read-only; `detach VOLUME` tears down; full option parsing: `format=N` (verity version 1 or 2), `hash=` (default sha256), `data-block-size=`/`hash-block-size=` (default 4096), `data-blocks=`, `hash-offset=`, `salt=HEX` (validated hex), `fec-device=PATH`/`fec-offset=`/`fec-roots=` (Forward Error Correction), `root-hash-signature=PATH`, `restart-on-corruption`/`ignore-corruption`/`panic-on-corruption` error modes, `ignore-zero-blocks`, `check-at-most-once`, `readonly`, `noauto`/`nofail`/`x-systemd.*` compat ignored; dm-verity target table construction with optional parameters (error mode, zero blocks, FEC, root hash signature); root hash hex validation; FEC device and signature file existence checks; 101 unit tests covering argument parsing (empty/help/version/attach minimal/with-options/missing args/detach/unknown), option parsing (empty/dash/none/format v1/v2/invalid/hash/block sizes/data-blocks/hash-offset/salt/salt-empty/salt-invalid/fec-device/fec-offset/fec-roots/root-hash-signature/error modes with underscore variants/ignore-zero-blocks/check-at-most-once/readonly/combined/noauto-nofail/systemd-extensions/unknown/whitespace/empty-parts/error-mode-override/fec-combined), hex validation (valid/invalid), verity params construction (default/salt/explicit-data-blocks/hash-offset/custom-algorithm/format-v2/all error modes/ignore-zero-blocks/check-at-most-once/fec/fec-default-roots/root-hash-signature/multiple-optional/eio-no-extra/custom-block-sizes), DM ioctl helpers (nr encoding/init/empty/long name/u32/u64/string/align8), target append (basic/params/alignment), integration (missing device/empty root hash/invalid root hash/nonexistent signature/nonexistent fec/detach nonexistent), full-flow (with hash options/with FEC), root hash signature read (ok/nonexistent/empty), constants
- ✅ **`integritysetup`** — dm-integrity data integrity block device setup via device-mapper ioctl interface; `attach VOLUME DEVICE [KEY [OPTIONS]]` sets up dm-integrity with DM_DEV_CREATE → DM_TABLE_LOAD → DM_DEV_SUSPEND (resume); `detach VOLUME` tears down; full option parsing: `algorithm=` (default crc32c, supports crc32c/sha1/sha256/sha512/xxhash64/blake2b/poly1305/hmac variants), `journal-commit-time=MS`, `journal-watermark=PCT` (0-100 validated), `journal-integrity=ALG`/`journal-integrity-key-size=`/`journal-integrity-key-file=PATH`, `journal-crypt=ALG`/`journal-crypt-key-size=`/`journal-crypt-key-file=PATH`, `data-device=PATH`, `sector-size=` (power-of-2 ≥512 validated, default 512), `bitmap-flush-interval=MS`, `block-size=`, `integrity-recalculate`/`integrity-recalculate-reset`, `allow-discards`/`discard`, `fix-padding`, `fix-hmac`, `legacy-recalculate`, `no-journal`/`bitmap`/`no-journal-bitmap` journal modes, `recovery`, `readonly`, `noauto`/`nofail`/`x-systemd.*` compat ignored; both hyphen and underscore variants accepted for all options; dm-integrity target table construction with journal mode character (J/D/B/R), tag size auto-detection from algorithm, optional parameters (internal_hash, key, sector size, block size, journal options, data device, bitmap flush, recalculate flags, discard, fix flags); HMAC key requirement validation; journal/crypt key file existence checks; 138 unit tests covering argument parsing (empty/help/version/attach minimal/key/key+options/missing args/detach/unknown), option parsing (empty/dash/none/algorithm/hmac/alias/journal-commit-time valid/invalid/journal-watermark valid/zero/100/over-100/invalid/journal-integrity/key-size/key-file/journal-crypt/key-size/key-file/data-device/sector-size valid/1024/not-power-of-2/too-small/nan/bitmap-flush-interval/block-size/integrity-recalculate/underscore/reset/allow-discards/discard-alias/fix-padding/fix-hmac/legacy-recalculate/no-journal/underscore/bitmap/no-journal-bitmap/recovery/readonly/read-only-alias/noauto-nofail/systemd-extensions/unknown/whitespace/empty-parts/combined/journal-mode-override/auto-ignored), tag size (crc32c/crc32/sha1/hmac-sha1/sha256/hmac-sha256/sha512/hmac-sha512/xxhash64/poly1305/blake2b-256/blake2b-512/unknown), key handling (hex encoding/read none/dash/empty/none-string/file/binary/nonexistent/empty-file), integrity params construction (default/sha256/hmac-with-key/no-journal/bitmap/recovery/recovery-overrides/recalculate/recalculate-reset/allow-discards/fix-padding/fix-hmac/legacy-recalculate/journal-options/data-device/bitmap-flush-interval/block-size/custom-sector-size/all-flags), DM ioctl helpers (nr encoding/init basic/readonly/suspend/empty/long name/u32/u64/string/align8), target append (basic/params/alignment), integration (missing device/hmac-without-key/nonexistent-data-device/nonexistent-journal-key-files/detach nonexistent), full-flow (with all options/minimal/journal options), constants; missing: format command (initialize integrity superblock), wipe command, dump command, resize command
- ✅ **`repart`** — declarative GPT partition manager with `.conf` file parsing (`[Partition]` section with Type=, Label=, UUID=, Priority=, Weight=, PaddingWeight=, SizeMinBytes=, SizeMaxBytes=, PaddingMinBytes=, PaddingMaxBytes=, FactoryReset=, Flags=, NoAuto=, ReadOnly=, GrowFileSystem=, Format=, CopyFiles=, CopyBlocks=, MakeDirectories=, Encrypt=, Verity=, VerityMatchKey=, SplitName=, Minimize=, SupplementFor=, MountPoint=, EncryptedVolume=, Compression=, CompressionLevel=), full GPT reading/writing (protective MBR, primary + backup headers, CRC32 validation, partition entry serialization/deserialization, UTF-16LE name encoding), partition type UUID database (ESP, swap, home, root/usr for x86-64/ARM64/etc., srv, var, tmp, xbootldr, linux-generic with architecture auto-detection), deterministic UUID generation from seed + name + counter (UUID v4 format), weighted space allocation with padding support (proportional distribution of free space among partitions based on weight, minimum/maximum size constraints, priority-based dropping when space insufficient), partition matching (existing partitions matched to definitions by type UUID, support for incremental updates), `mkfs` integration (ext4, vfat, swap, xfs, btrfs), empty mode handling (refuse/allow/require/force/create), factory reset support, dry-run mode, JSON output (short/pretty), `--list-devices` block device enumeration, `--make-ddi` DDI image creation (sysext/confext/portable), `--include-partitions`/`--exclude-partitions`/`--defer-partitions` filtering, `--seed` deterministic UUIDs, `--size` image sizing (auto-calculation from definitions), `--sector-size` (512/4096), `--architecture`, `--root`, `--copy-source`, `--key-file`, `--offline`, `--split`; 200+ unit tests covering argument parsing, size parsing/formatting, GUID encode/decode roundtrips, UUID generation (deterministic/random/version bits), CRC32, partition type UUIDs (all known types + architecture resolution), definition parsing (all fields, swap, home, comments, empty resets, unknown sections, UUID modes, weight validation, format types, copy files/blocks with reset, encrypt/verity/minimize/flags/mount point/compression/supplement/split), definition loading (empty/nonexistent/sorted/skip non-conf/dedup), effective flags (no-auto/read-only/grow-fs/override/combined), effective labels (explicit/from type/fallback), UTF-16LE encoding, partition matching (all new/existing matched/mixed/multiple same type/name handling), free region finding (empty disk/one partition/full/gap between/multiple gaps/at start/at end), space allocation (single/two weighted/fixed size/seed UUID/explicit UUID/with padding/insufficient space/priority dropping), GPT write/read roundtrip (single/multiple/attributes/empty table/protective MBR/header CRC32/backup header), full integration (create from definitions/dry run/refuse empty/size query/can factory reset yes/no/JSON output/pretty output/no definitions/incremental/force clears/require rejects existing/factory reset removes), sector size 4096, JSON pretty output, alignment, partition entry layout, header field validation
- ✅ **`sysext`** — system extension image overlay management with `status` (show merge state and active extensions), `list` (enumerate available extensions from `/run/extensions/`, `/var/lib/extensions/`, `/usr/lib/extensions/`, `/usr/local/lib/extensions/`), `merge` (overlayfs-based merging of extension hierarchies `/usr/` and `/opt/`), `unmerge` (tear down overlayfs mounts), `refresh` (unmerge + merge), `check-inhibit` (check for inhibitor), JSON output modes (short/pretty), extension release file parsing with host compatibility checking (ID, VERSION_ID, SYSEXT_LEVEL, ARCHITECTURE, SYSEXT_SCOPE), hierarchy detection, merge marker tracking, `--root`/`--force`/`--no-reload`/`--json` options; 80 unit tests
- ✅ **`dissect`** — disk image inspection tool with GPT and MBR partition table parsing, `show` (detailed image info with partition types, GUIDs, sizes, attributes), `list` (partition listing), `discover` (scan image search paths), `validate` (check partition table integrity), `mount`/`umount` (loopback mount/unmount), `copy-from`/`copy-to` (stubs), JSON output modes (short/pretty), known GPT partition type database (root/home/srv/swap/ESP/XBOOTLDR for x86-64/ARM64/etc.), CRC32 header validation, UTF-16LE partition name parsing, human-readable size formatting, `--root-hash`/`--verity-data`/`--no-legend` options; 100+ unit tests
- ✅ **`firstboot`** — initial system configuration wizard with `--locale`, `--keymap`, `--timezone`, `--hostname`, `--machine-id`, `--root-password`/`--root-password-hashed`, `--root-shell`, `--kernel-cmdline` settings; `--prompt`/`--prompt-*` interactive modes; `--copy-*` to copy host settings; `--reset-*` to clear settings; `--root` for chroot operation; `--force` to overwrite existing; `--delete-root-password` to unlock root; `--setup-machine-id` alias; `--welcome` banner; credential loading from `$CREDENTIALS_DIRECTORY`; system-already-booted detection; locale/keymap/timezone/shell enumeration; SHA-512 password hashing; proper `/etc/passwd` and `/etc/shadow` manipulation; 100+ unit tests
- ✅ **`creds`** — credential encryption/decryption tool with `list` (enumerate credentials with size/security state), `cat` (show credential contents with transcode options), `setup` (generate host encryption key), `encrypt` (AES-256-GCM with host key or null key, Base64 output, `--pretty` for unit file embedding), `decrypt` (with name validation, expiry checking, `--allow-null`), `has-tpm2` (TPM2 device detection); custom wire format compatible with systemd's credential header; runtime decryption integrated into exec helper for `LoadCredentialEncrypted=`/`SetCredentialEncrypted=`; missing: TPM2 sealing, host+tpm2 combined mode
- ✅ **`inhibit`** — inhibitor lock tool with acquire/release/list, block/delay modes, stale lock cleanup