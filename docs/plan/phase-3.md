# Phase 3 — Network Stack

Full network management:

- 🔶 **`networkd`** — network configuration daemon with `.network` file parsing ([Match], [Network], [Address], [Route], [RoutingPolicyRule], [DHCPv4], [Link] sections), **`.link` file parsing** (`systemd.link(5)` with [Match] section: OriginalName, MACAddress, Path, Driver, Type, Property, Host, Virtualization, KernelCommandLine, KernelVersion, Architecture, Credential; [Link] section: Name, NamePolicy with kernel/database/onboard/slot/path/mac/keep, AlternativeNamesPolicy, AlternativeName, MACAddressPolicy with persistent/random/none, MACAddress, MTUBytes, BitsPerSecond, Duplex, AutoNegotiation, WakeOnLan, Port, offload settings, channel settings, Unmanaged, RequiredForOnline, ActivationPolicy; `load_link_configs()`/`load_link_configs_from()` with standard search paths + NixOS package-relative, dedup by filename, sorted lexicographically; `find_matching_link_config()` first-match with AND logic; 51 unit tests), **`.netdev` file parsing** (`systemd.netdev(5)` for virtual network device definitions with [NetDev] section: Name, Kind, Description, MTUBytes, MACAddress; [Match] section: Host, Virtualization, KernelCommandLine, KernelVersion, Architecture, Credential; 35 device kinds supported: bridge, bond, vlan, vxlan, wireguard, macvlan, macvtap, ipvlan, ipvtap, veth, tun, tap, dummy, ifb, gre, gretap, ip6gre, ip6gretap, sit, vti, vti6, ip6tnl, erspan, geneve, bareudp, l2tp, macsec, xfrm, batadv, fou, wlan, nlmon, vrf, vcan, vxcan; kind-specific sections: [Bridge] with STP/ForwardDelaySec/HelloTimeSec/MaxAgeSec/AgeingTimeSec/Priority/GroupForwardMask/DefaultPVID/MulticastQuerier/MulticastSnooping/VLANFiltering/VLANProtocol/MulticastIGMPVersion, [Bond] with Mode (7 modes including 802.3ad)/TransmitHashPolicy (6 policies), [VLAN] with Id/Protocol/GVRP/MVRP/LooseBinding/ReorderHeader/EgressQOSMaps/IngressQOSMaps, [VXLAN] with VNI/Remote/Group/Local/TOS/TTL/MacLearning/FDBAgeingSec/DestinationPort/MaximumFDBEntries/UDP6ZeroChecksumRx/UDP6ZeroChecksumTx/PortRange/FlowLabel/L2Miss/L3Miss/ReduceARPProxy/RouteShortCircuit/GroupPolicyExtension/GenericProtocolExtension/Independent, [WireGuard] with PrivateKey/PrivateKeyFile/ListenPort/FwMark/RouteTable/RouteMetric, [WireGuardPeer] (multiple) with PublicKey/PresharedKey/PresharedKeyFile/Endpoint/AllowedIPs/PersistentKeepalive/RouteTable/RouteMetric, [Tunnel] with , [MACVLAN]/[MACVTAP] with Mode (5 modes)/SourceMACAddress, [IPVLAN] with Mode (L2/L3/L3S)/Flags (bridge/private/vepa), [Tun]/[Tap] with MultiQueue/PacketInfo/VNetHeader/User/Group/KeepCarrier, [Peer] with Name/MACAddress, [FOU] with Encapsulation/Port/PeerPort/Local/Peer/Protocol, [Geneve] with VNI/Remote/TOS/TTL/DestinationPort/FlowLabel/UDP6ZeroChecksumTx/Rx, [L2TP] with TunnelId/PeerTunnelId/Remote/Local/EncapsulationType/UDPSourcePort/UDPDestinationPort/UDPChecksum, [L2TPSession] (multiple) with SessionId/PeerSessionId/Name/L2SpecificHeader, [MACsec] with Port/Encrypt, [BatmanAdvanced] with GatewayMode/Aggregation/BridgeLoopAvoidance/DistributedArpTable/Fragmentation/HopPenalty/OriginatorIntervalSec/GatewayBandwidthDown/GatewayBandwidthUp/RoutingAlgorithm, [BareUDP] with DestinationPort/EtherType, [XFRM] with InterfaceId/Independent, [VRF] with Table; `load_netdev_configs()`/`load_netdev_configs_from()` with standard search paths + NixOS package-relative, dedup by filename, sorted lexicographically; loaded alongside `.network` files in NetworkManager; 82 unit tests), **`.netdev` virtual device creation** via netlink RTM_NEWLINK for 30 device kinds with full kind-specific IFLA_INFO_DATA, DHCPv4 client with full DORA state machine (discover/offer/request/ack, lease renewal T1/T2, rebinding, exponential backoff retransmission, classless static routes RFC 3442, release/decline/inform), static IPv4 address and route configuration, **IPv6 Router Advertisement handling** with full `ipv6_ra` module — ICMPv6 RS/RA socket (IPPROTO_ICMPV6 raw socket with hop limit 255, ICMP6_FILTER for RA-only, SO_BINDTODEVICE per-interface, IPV6_RECVPKTINFO/IPV6_RECVHOPLIMIT, non-blocking), Router Solicitation construction and sending to ff02::2 all-routers multicast (with Source Link-Layer Address option), RA message parsing (RFC 4861 §4.2: cur_hop_limit, M/O flags, router_lifetime, reachable_time, retrans_timer), NDP option parsing — Prefix Information (RFC 4861 §4.6.2: prefix_len, on-link/autonomous flags, valid/preferred lifetime, /64 prefix), RDNSS (RFC 8106 §5.1: lifetime, multiple IPv6 DNS server addresses), DNSSL (RFC 8106 §5.2: lifetime, DNS-encoded domain names), Route Information (RFC 4191 §2.3: prefix, preference, lifetime, variable-length prefix encoding), MTU option (RFC 4861 §4.6.4), Source Link-Layer Address option; **SLAAC address generation** via EUI-64 (insert ff:fe in MAC, flip U/L bit, combine with /64 prefix) and **RFC 7217 stable privacy addresses** (SHA-256 hash of prefix/ifname/DAD-counter/secret-key, secret from `/etc/machine-id` or `IPv6StableSecretAddress=` config, U/L bit cleared for local scope, `Ipv6AddressGenMode` enum with Eui64/StablePrivacy variants, `slaac_stable_privacy()` and `stable_privacy_link_local()` functions), link-local address generation (EUI-64 fe80::<eui64> or stable privacy fe80::<hash>); **IPv6 address lifetime management** — `SlaacAddress` struct tracks per-address valid/preferred lifetimes with `Instant`-based timers, `is_expired()`/`is_deprecated()`/`remaining_valid()`/`remaining_preferred()` queries, `refresh_lifetimes()` implements RFC 4862 §5.5.3e two-hour rule (new valid >2h → accept, remaining >2h → don't reduce, else min(received, 2h); preferred always updated), `RaState::check_address_lifetimes()` periodic scan produces `RemoveAddress`/`DeprecateAddress` actions, expired addresses removed via `remove_ipv6_address()` (netlink RTM_DELADDR), valid_lifetime=0 in RA immediately removes address, `NetworkManager::check_ra_address_lifetimes()` integrated into main event loop; `IPv6StableSecretAddress=` config option (hex secret or "machine-id"), `IPv6LinkLocalAddressGenerationMode=` config option (eui64/stable-privacy), `read_machine_id_as_bytes()` helper; IPv6 netlink operations — `add_ipv6_address()` via RTM_NEWADDR with AF_INET6, `remove_ipv6_address()` via RTM_DELADDR with AF_INET6, `add_ipv6_route()`/`add_ipv6_default_route()` via RTM_NEWROUTE with AF_INET6 and RTPROT_RA; `RaState` per-link state tracking (RS count/timing with RFC 4861 §6.3.7 retransmit interval, received RA storage, SLAAC address list with lifetime tracking, `addr_gen_mode`/`dad_counter` for RFC 7217, default router, DNS/search domains from RDNSS/DNSSL); `RaAction` enum drives manager to add/remove/deprecate addresses, add routes/DNS/MTU; RS retransmission (max 3, 4s interval); `IPv6AcceptRA=yes` config support; integrated into main event loop alongside DHCP (open ICMPv6 sockets, send initial RS, poll for RA, apply actions, retransmit RS, periodic lifetime checks); 119 unit tests in ipv6_ra module (83 existing + 36 new for RFC 7217 and lifetime management); netlink-based interface management (RTM_NEWLINK/GETLINK/NEWADDR/DELADDR/NEWROUTE/DELROUTE/NEWRULE/DELRULE, bring up/down, set MTU, flush addresses/routes), DNS resolver configuration (`/run/systemd/resolve/resolv.conf`), runtime state files (`/run/systemd/netif/links/`, `/run/systemd/netif/leases/`, `/run/systemd/netif/state`), **PID file** at `/run/systemd/netif/systemd-networkd.pid` (written after READY=1, removed on shutdown, used by `networkctl` for daemon discovery), sd_notify protocol (READY/WATCHDOG/STATUS/STOPPING), signal handling (SIGTERM/SIGINT for shutdown, SIGHUP for reload), NixOS integration enabled (`withNetworkd = true`); **D-Bus interface (`org.freedesktop.network1`)** with Manager object (ListLinks, GetLinkByName, GetLinkByIndex, Describe, Reload, ForceRenew; properties: OperationalState, CarrierState, AddressState, OnlineState, NamespaceId); **functional D-Bus Reload() and ForceRenew()** — `SignalFlags` struct with `AtomicBool`/`AtomicI32` shared between D-Bus interface and main event loop via `Arc<SignalFlags>`; D-Bus Reload() sets `reload` flag consumed by main loop (same path as SIGHUP); D-Bus ForceRenew(ifindex) stores target ifindex (or -1 for all links) — main loop resets DHCP client to Init state and sends fresh DHCPDISCOVER; deferred D-Bus registration (connects after READY=1 to avoid blocking early boot); shared state via `Arc<Mutex<>>` updated on link changes, DHCP events, and reload; `networkctl` CLI with `list` (interface table with type/operational/setup state), `status [LINK]` (detailed per-link info including address/gateway/DNS/DHCP lease), **`reload`** (finds networkd PID from PID file or /proc scan, sends SIGHUP to trigger config reload), **`reconfigure LINK...`** (validates interfaces via sysfs, sends SIGHUP for full reconfiguration), **`forcerenew LINK...`** (validates interfaces, sends SIGHUP to trigger DHCP renewal), `lldp` (stub); PID discovery via `find_networkd_pid()` with PID file fallback to /proc comm scanning; `resolve_ifindex()` for name-to-ifindex resolution via sysfs; 19 unit tests in networkctl (10 new for PID discovery and ifindex resolution); missing: DHCPv6-PD, `systemd-network-generator`
- 🔶 **`resolved`** — stub DNS resolver daemon with `resolved.conf` parsing ([Resolve] section with DNS, FallbackDNS, Domains, LLMNR, MulticastDNS, DNSSEC, DNSOverTLS, Cache, DNSStubListener, DNSStubListenerExtra, ReadEtcHosts, ResolveUnicastSingleLabel, CacheFromLocalhost) and drop-in directory support, stub DNS listener on 127.0.0.53:53 (UDP + TCP), DNS query forwarding to upstream servers (UDP with TCP fallback on truncation, per-server retry with exponential backoff), DNS wire format parsing (RFC 1035 headers, question sections, domain name compression with loop detection), `/run/systemd/resolve/stub-resolv.conf` management (points to 127.0.0.53), `/run/systemd/resolve/resolv.conf` management (lists upstream servers), per-link DNS configuration from networkd state files (`/run/systemd/netif/links/`), periodic link DNS refresh, atomic file writes for resolv.conf, **PID file** at `/run/systemd/resolve/systemd-resolved.pid` (written after READY=1, removed on shutdown, used by `resolvectl` for daemon discovery), sd_notify protocol (READY/WATCHDOG/STATUS/STOPPING), signal handling (SIGTERM/SIGINT for shutdown, SIGHUP for reload/cache flush), query statistics tracking, multi-threaded listener architecture; **split DNS routing** via `DnsRouter` — per-query upstream server selection based on routing domains (prefixed with `~` in `Domains=` configuration); longest-suffix domain matching (e.g., `~corp.example.com` routes `host.corp.example.com` to the link's DNS servers); catch-all routing domain (`~.`) for VPN full-tunnel; same-specificity server merging across links; `DefaultRoute=yes` links receive unmatched queries; priority order: specific routing match → catch-all → default-route → global DNS → fallback DNS; global routing domains from `resolved.conf`; per-link routing from networkd state files; case-insensitive matching; backward compatible (no routing domains = flat upstream list); router rebuilt on reload/link refresh; `extract_query_name()` for DNS wire format name extraction; 48 unit tests (42 in routing module + 6 integration); **`/etc/hosts` file reading** (`ReadEtcHosts=yes` default) with `EtcHosts` struct providing full `/etc/hosts` parser and DNS response builder — parses IPv4 and IPv6 addresses with multiple hostnames per line, comment stripping, case-insensitive lookups; answers A, AAAA, PTR (IPv4 `in-addr.arpa` and IPv6 `ip6.arpa` nibble format), and ANY queries directly from the hosts database with proper DNS wire format responses (AA=1, TTL=0 matching real systemd); `/etc/hosts` lookups take priority over DNS cache and upstream forwarding; reverse lookups use the first hostname on the line as the canonical name (first entry wins for duplicate addresses); periodic 2-second file change detection via mtime comparison with automatic reload; SIGHUP triggers immediate reload; shared across listener threads via `Arc<Mutex<EtcHosts>>`; graceful fallback when `/etc/hosts` is missing or unreadable; 62 unit tests (56 in hosts module + 6 integration); **in-memory DNS cache** with TTL-based expiration (cache key: lowercased name + qtype + qclass; caches NOERROR and NXDOMAIN responses; TTL from minimum RR TTL clamped to 1s–3600s; transaction ID rewriting on hits; max 4096 entries with expired-first/oldest-first eviction; `DnsCache` with lookup/insert/flush; `CacheStats` with lookups/hits/misses/inserts/evictions/flushes and hit_rate(); SIGHUP flushes cache; 47 unit tests); **D-Bus interface (`org.freedesktop.resolve1`)** with Manager object (FlushCaches — now actually flushes the DNS cache, ResetStatistics, GetLink, SetLinkDNS, SetLinkDomains, RevertLink, Describe; properties: DNS, FallbackDNS, Domains, LLMNR, MulticastDNS, DNSSEC, DNSOverTLS, DNSStubListener, Cache, DNSSECSupported, TransactionStatistics, CacheStatistics — now returns live cache size/hits/misses); deferred D-Bus registration (connects after READY=1 to avoid blocking early boot); shared state via `Arc<Mutex<>>` updated on config reload and link DNS refresh; `resolvectl` CLI with `status [LINK...]` (global and per-link DNS status, resolv.conf mode detection, search domains), `query HOSTNAME...` (hostname resolution via system resolver with DNS stub fallback, A + AAAA queries), `statistics` (resolver statistics), `flush-caches` (sends SIGHUP to resolved), `reset-statistics`, `dns` (show/set per-link DNS), `domain` (show/set per-link domains), `llmnr`/`mdns`/`dnssec`/`dnsovertls` (show per-link settings), **`revert LINK...`** (validates interfaces via sysfs including numeric ifindex lookup, sends SIGHUP to resolved to revert per-link DNS overrides back to networkd-provided state; 7 new unit tests for interface validation and PID file parsing), legacy `systemd-resolve` interface when invoked as `systemd-resolve`; `networkctl persistent-storage` subcommand for NixOS `systemd-networkd-persistent-storage.service` compatibility; NixOS integration prepared (disabled pending PID 1 unit alias handling fix); missing: DNS-over-TLS, DNSSEC validation, mDNS responder/resolver, LLMNR responder/resolver, EDNS0 client subnet, `resolvectl` monitor, negative trust anchors
- ✅ **`networkd-wait-online`** — network online wait tool for `network-online.target` integration; monitors networkd state files (`/run/systemd/netif/links/` per-link and `/run/systemd/netif/state` overall) to determine when the network is fully configured; operational state ordering (off < no-carrier < dormant < degraded-carrier < carrier < degraded < enslaved < routable); filters out unmanaged links (no `NETWORK_FILE=` in state file); resolves interface names from sysfs (`/sys/class/net/*/ifindex`); `--timeout=SECS` (default 120s), `--interface=IFACE[:STATE]` (wait for specific interfaces with optional per-interface state), `--ignore=IFACE` (exclude interfaces), `--any` (succeed when any single qualifying link is online), `--operational-state=STATE` (minimum required state, default degraded), `--quiet`/`-q`; sd_notify READY/STATUS protocol; 80 unit tests
- ✅ **`timesyncd`** — SNTP time synchronization daemon with NTP v4 client, `timesyncd.conf` parsing with drop-in directories, clock adjustment (slew via `adjtimex()` for small offsets, step via `clock_settime()` for large), clock state persistence, sd_notify protocol, signal handling, exponential backoff polling, container detection, graceful degradation; **D-Bus interface (`org.freedesktop.timesync1`)** with Manager object (Describe; properties: NTPSynchronized, ServerName, ServerAddress, Frequency, RootDistanceMaxUSec, PollIntervalMinUSec, PollIntervalMaxUSec, PollIntervalUSec); deferred D-Bus registration (connects after READY=1 to avoid blocking early boot); shared state via `Arc<Mutex<>>` updated on sync events and config reload; `timedatectl` CLI with `status`, `show`, `set-time`, `set-timezone`, `set-ntp`, `list-timezones`, `timesync-status`; missing: NTS support
- ✅ **`timedated`** — time and date management daemon managing timezone (`/etc/localtime` symlink + `/etc/timezone`), RTC local/UTC mode (`/etc/adjtime`), NTP enable/disable (controls `systemd-timesyncd.service`); timezone detection from multiple sources, timezone validation with path traversal protection, timezone listing with IANA filtering; control socket at `/run/systemd/timedated.sock`; **D-Bus interface (`org.freedesktop.timedate1`)** with properties (Timezone, LocalRTC, CanNTP, NTP, NTPSynchronized, TimeUSec, RTCTimeUSec) and methods (SetTime, SetTimezone, SetLocalRTC, SetNTP, ListTimezones); deferred D-Bus registration (connects after READY=1 to avoid blocking early boot); sd_notify protocol, watchdog keepalive, signal handling; NixOS integration enabled (`withTimedated = true`); `timedatectl` queries state directly and communicates via control socket for mutations; 72 unit tests
- ✅ **`hostnamed`** — hostname management daemon with static/pretty/transient hostname support, `/etc/hostname` and `/etc/machine-info` management, DMI chassis auto-detection, control socket, watchdog keepalive; **D-Bus interface (`org.freedesktop.hostname1`)** with properties (Hostname, StaticHostname, PrettyHostname, IconName, Chassis, Deployment, Location, KernelName, KernelRelease, OperatingSystemPrettyName, OperatingSystemCPEName, OperatingSystemHomeURL, HardwareVendor, HardwareModel, HostnameSource) and methods (SetHostname, SetStaticHostname, SetPrettyHostname, SetIconName, SetChassis, SetDeployment, SetLocation, GetProductUUID, Describe); deferred D-Bus registration; NixOS integration enabled (`withHostnamed = true`); `hostnamectl` CLI with `status`, `show`, `hostname`, `set-hostname`, `chassis`, `deployment`, `location`, `icon-name` commands; 65 unit tests
- ✅ **`localed`** — locale and keyboard layout management daemon with `/etc/locale.conf`, `/etc/vconsole.conf`, and X11 keyboard configuration management, keymap/layout listing, control socket, watchdog keepalive; **D-Bus interface (`org.freedesktop.locale1`)** with properties (Locale, X11Layout, X11Model, X11Variant, X11Options, VConsoleKeymap, VConsoleKeymapToggle) and methods (SetLocale, SetVConsoleKeyboard, SetX11Keyboard); deferred D-Bus registration; NixOS integration enabled (`withLocaled = true`); `localectl` CLI with `status`, `show`, `set-locale`, `set-keymap`, `set-x11-keymap`, `list-keymaps`, `list-x11-keymap-*` commands; 49 unit tests; missing: automatic keymap-to-X11 conversion